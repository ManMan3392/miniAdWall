// Minimal auto-import config for ESLint.
// `eslint.config.js` expects a default export that can be spread into
// the `extends` array. Provide an empty array as a safe fallback.
// Replace or expand this file with your real auto-imports if needed.
export default [];
